<h2>New engine</h2>
  <% if user_signed_in? %>
    <% if current_user.admin? %>
      <%= link_to 'Back', session[:url], :class => "pull-right btn btn-primary btn-lg", :role =>"button" %><br/><br/>
    <% else %>
      <%= link_to 'Back', session[:url], :class => "pull-right btn btn-primary btn-lg", :role =>"button"%><br/><br/> 
    <% end %>
  <% end %>
  <div class="well well-lg">
    <%= render_breadcrumbs %>
    <div class="well well-large alert alert-info">  
     <%= render 'form' %>
    </div>
  </div>
